#include <stdio.h>
int main()
{
    int x,y,*xptr,*yptr;
    if (scanf("%d", &x) != 1 || x < 1 || x > 100){
        printf("INVALID INPUT\n");
    }else if(scanf("%d", &y) != 1 || y < 1 || y > 100){
        printf("INVALID INPUT\n");
    }
       xptr = &x;
     yptr = &y;
    if (*xptr>*yptr) printf("%d", *xptr);
    else printf("%d", *yptr);
	return 0;
}
